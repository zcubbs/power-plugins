version: '3'

tasks:
  ############################################################
  default:
    desc: Run Build
    cmds:
      - cmd: go build -o bin/{{.ITEM}} ./plugins/{{.ITEM}}/...
        for: ['go_apiserver']
