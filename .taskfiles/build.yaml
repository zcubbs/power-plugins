version: '3'

tasks:
  ############################################################
  default:
    desc: Run Build
    cmds:
      - cmd: go build -o bin/{{.ITEM}} ./plugins/{{.ITEM}}/main.go
        for: ['go-apiserver', 'java-springbootapp']
